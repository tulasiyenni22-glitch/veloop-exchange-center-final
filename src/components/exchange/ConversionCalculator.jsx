import { ArrowRight } from 'lucide-react';
import InfoTip from './InfoTip';
import gemArtwork from '../../assets/gem.png';
import styles from './Exchange.module.css';
export default function ConversionCalculator({gems,setGems,onPreview,max}){const rate=5.39,ves=Math.floor((Number(gems)||0)*rate);return <section className={styles.calculator}><h2>Enter Gems Amount</h2><div className={styles.calcRow}><label><img className={styles.miniGem} src={gemArtwork} alt=""/><span><input aria-label="Gems to convert" min="0" max={max} type="number" value={gems} onChange={e=>setGems(e.target.value)}/><small>Gems to convert</small></span><button type="button" onClick={()=>setGems(max)}>MAX</button></label><span className={styles.arrow}><ArrowRight/></span><div className={styles.output}><span className={styles.veIcon}>VE</span><span><strong>{ves.toLocaleString()}</strong><small>VEs you will receive</small></span></div><button className={styles.primary} type="button" disabled={!Number(gems)} onClick={()=>onPreview({requiredGems:Number(gems),receiveVEs:ves,label:'Custom Conversion'})}>Preview Conversion <ArrowRight size={18}/></button></div><p className={styles.rate}>1 Gem = 5.39 VEs <InfoTip label="This sample exchange value is predefined by VELOOP Rewards."/></p></section>}
